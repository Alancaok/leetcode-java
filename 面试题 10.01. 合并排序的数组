class Solution {
    public void merge(int[] A, int m, int[] B, int n) {
        int[] res=new int[m];
        int index=0;
        for(int i=0;i<n;i++)
        {
            A[m++]=B[i];
        }
        Arrays.sort(A);

    }
}
